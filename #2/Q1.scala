object Q1 {
  def main(args:Array[String]): Unit ={
    var k, i, j = 2;
    var m, n = 5;
    var f = 12.0F;
    var g = 4.0F;
    var c = 'X';

    println("k + 12 * m = " + (k + 12 * m));
    println("m / j = " + (m / j));
    println("n % j = " + (n % j));
    println("m / j * j = " + (m / j * j));
    println("f + 10*5 +g = " + (f + 10*5 +g));
    //println("++i * n = " + (++i * n)); -> Prefix and postfix operations are not supported in Scala.
  }
}
